import Phaser from 'phaser';

class test extends Phaser.Scene {
    constructor() {
        super();
    }

    preload() {
        
    }

    create() {
        
    }
}

    const config = {
        type: Phaser.AUTO,
        width: 800,
        height: 600,
        scene: test
    };

    const game = new Phaser.Game(config);